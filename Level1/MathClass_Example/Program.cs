using System;
namespace MathClass_Example
{
    class Program
    {
        static void Main(string[] args)
        {
            double number = 16.0;
            double squareRoot = Math.Sqrt(number); // using Math class to calculate square root
            Console.WriteLine("Square root of " + number + " is: " + squareRoot);
        }
    }
}